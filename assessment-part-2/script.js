const negButton = document.querySelector('.negative-button')
const posButton = document.querySelector('.positive-button')
let input = input
let value = value
const displayWindow = (negButton.innerHTML = 'negative')
negButton.addEventListener('click', function () {
  if (negButton === true) {
    let input = `${input}-`
  }
  console.log(displayWindow)
})
document.body.appendChild(negButton)
{
}
posButton.innerHTML = 'positive'
posButton.addEventListener('click', function () {
  if (posButton === true) alert(`positive button is clicked`)
})
document.body.appendChild(posButton)
